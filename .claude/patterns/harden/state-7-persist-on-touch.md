# STATE 7: PERSIST_ON_TOUCH

**PRECONDITIONS:**
- STATE 6 POSTCONDITIONS met (reconciliation complete)

**ACTIONS:**

Follow ON-TOUCH pattern per `patterns/on-touch-check.md`.

**Persist ON-TOUCH list**: Write the ON-TOUCH module list from STATE 1 to `experiment/on-touch.yaml`:
```yaml
# Auto-generated by /harden. Modules to harden when next modified.
# /change reads this file in production mode.
on_touch:
  - path: src/app/api/invoices/route.ts
    reason: "Read-only GET, no mutations"
```
Remove entries when a module gets spec tests via `/change` or a subsequent `/harden` run.

Update checkpoint to `step3-verify`.

**POSTCONDITIONS:**
- `experiment/on-touch.yaml` exists with ON-TOUCH module list
- Checkpoint updated to `step3-verify`

**VERIFY:**
```bash
test -f experiment/on-touch.yaml && echo "OK" || echo "FAIL"
```

**STATE TRACKING:** After postconditions pass, mark this state complete:
```bash
bash .claude/scripts/advance-state.sh harden 7
```

**NEXT:** Read [state-8-verify.md](state-8-verify.md) to continue.
